# Демо-версия для сайта. Rasa/Rasa SDK ставятся отдельно (как в исходном проекте):
#   python -m pip install "rasa>=3.6,<4.0" "rasa-sdk>=3.6,<4.0"
#   python -m pip install -r requirements.txt
#
# ВАЖНО: numpy зафиксирован <1.25 — Rasa 3.x НЕ работает с numpy 2.x.
# fastapi/uvicorn зафиксированы на версиях с pydantic 1.x, т.к. Rasa 3.x требует
# pydantic<1.10.10 (более новые fastapi тянут pydantic 2.x и ломают Rasa).
#
# По сравнению с боевой версией убраны: requests и python-docx (были нужны только
# интеграциям WhatsApp/CRM, которых в демо нет).

httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
rapidfuzz>=3.0,<4.0
numpy>=1.23,<1.25
openai>=1.0.0,<2.0
fastapi<0.100
uvicorn[standard]<0.21
